# Лучшие практики

Brick — это **UI-слой**, и его задача предельно проста: превратить данные в HTML. Компонент не должен ничего знать о бизнес-логике, базах данных или внешних сервисах. Он принимает данные и возвращает разметку — и ничего больше.

---

## 1. Компонент = Функция от данных

Хороший компонент — это **чистая функция**:
- Принимает данные через конструктор
- Возвращает HTML
- Не имеет побочных эффектов

```php
// Чистый UI-компонент
class ProductCard extends Brick
{
    public function __construct(
        public string $title,
        public string $price,
        public string $imageUrl
    ) {}
}
```

Компонент **не знает**, откуда эти данные. Ему всё равно.

---

## 2. Нет запросам внутри компонента

Компонент не должен:
- Делать SQL-запросы
- Вызывать API
- Работать с файловой системой
- Использовать глобальные переменные или сессии напрямую

**✅ Правильно:** данные передаются извне  
**❌ Неправильно:** компонент сам добывает данные

```php
// ❌ ПЛОХО: компонент делает запрос
class UserCard extends Brick
{
    public function __construct(public int $userId)
    {
        $this->user = User::find($this->userId); // НЕТ!
    }
}

// ✅ ХОРОШО: данные уже готовы
class UserCard extends Brick
{
    public function __construct(
        public string $userName,
        public string $userAvatar
    ) {}
}
```

---

## 3. Шаблоны — только отображение

Шаблон должен содержать **минимальную логику**:
- Ветвления (`if`)
- Циклы (`foreach`)
- Простые преобразования данных

Всё сложное — в классе или лучше вообще вне компонента.

```php
// ✅ Просто и ясно
<?php if ($this->isOnSale): ?>
    <span class="sale-badge">Sale</span>
<?php endif; ?>

// ❌ Слишком много логики
<?php 
    $discount = calculateDiscount($this->product);
    $finalPrice = $this->price - $discount;
    // ... и ещё 10 строк
?>
```

---

## 4. Без состояния (stateless)

Компонент не хранит состояние между рендерингами:
- Каждый рендер — независим
- Нет внутренних изменяемых данных
- Результат зависит только от входных свойств

---

## 5. Переиспользуемость через простоту

Чем проще компонент, тем легче его переиспользовать:
- Не завязывайте на конкретную бизнес-логику
- Не используйте доменные термины в нейминге (если это не UI-термин)
- Делайте компоненты универсальными

```php
// ✅ Универсально
class Badge extends Brick
{
    public function __construct(
        public string $text,
        public string $variant = 'default'
    ) {}
}

// ❌ Привязано к домену
class ShoppingCartItemBadge extends Brick
{
    // Теперь это только для корзины
}
```

---

## 6. Тестирование: только рендеринг

Тестируйте только то, что компонент **делает**:
- Корректность HTML-разметки
- Экранирование данных
- Обработку разных вариантов свойств

```php
public function testBadgeRendersCorrectText()
{
    $badge = new Badge('New', 'success');
    $html = $badge->render();
    
    $this->assertStringContainsString('New', $html);
    $this->assertStringContainsString('badge-success', $html);
}
```

Не тестируйте интеграции — это не ответственность компонента.

---

## 7. Иерархия данных

Если компонент сложный — разбивайте данные на подструктуры:

```php
class ProductGrid extends Brick
{
    /**
     * @param ProductCardData[] $products
     */
    public function __construct(
        public array $products
    ) {}
}

// Отдельный DTO для данных карточки
class ProductCardData
{
    public function __construct(
        public string $title,
        public string $price,
        public string $imageUrl
    ) {}
}
```

---

## 8. Провайдеры данных — отдельно

Создавайте отдельные классы для подготовки данных:

```php
// ❌ Не в компоненте
class ProductPage extends Brick
{
    public function __construct()
    {
        $this->products = Product::all(); // НЕТ!
    }
}

// ✅ Отдельный провайдер
class ProductDataProvider
{
    public function getProducts(): array
    {
        return Product::all()->map(fn($p) => new ProductCardData(
            $p->title,
            $p->price,
            $p->image_url
        ))->toArray();
    }
}

// В контроллере или сервисе:
$provider = new ProductDataProvider();
$products = $provider->getProducts();
$component = new ProductGrid($products);
```

---

## 9. Границы ответственности

Brick отвечает за:
- Генерацию HTML
- Обработку UI-событий (через script.js)
- Стилизацию (через style.css)

Brick **не** отвечает за:
- Бизнес-логику
- Работу с данными
- Маршрутизацию
- Аутентификацию

---

## 10. KISS: Keep It Simple, Stupid

Самый важный принцип: **делайте тупо**.

Если кажется, что нужно добавить логику в компонент — скорее всего:
1. Нужно передать больше данных извне
2. Нужно создать более простой компонент
3. Нужно вынести логику в другой слой

---

## Антипаттерны UI-слоя

### 1. **Компонент с бизнес-логикой**
```php
class OrderButton extends Brick
{
    public function __construct(public int $orderId)
    {
        // ❌ Проверяет статус заказа
        $this->canCancel = Order::find($orderId)->status === 'pending';
    }
}
```

### 2. **Компонент-сервис**
```php
class WeatherWidget extends Brick
{
    public function __construct()
    {
        // ❌ Ходит в API за погодой
        $this->weather = $httpClient->get('https://api.weather.com/...');
    }
}
```

### 3. **Компонент с состоянием сессии**
```php
class UserMenu extends Brick
{
    public function __construct()
    {
        // ❌ Работает с сессией напрямую
        $this->userName = $_SESSION['user_name'];
    }
}
```

---

## Золотое правило UI-компонента

> Компонент должен быть настолько тупым, чтобы его можно было заменить другим без переписывания всего.

Если вы уберёте Brick и будете использовать другой UI-инструмент — компоненты должны остаться простыми преобразователями данных в HTML.

---

## Что дальше?

🚀 **Готово!** Вы освоили все возможности Brick.
Возвращайтесь к [документации →](index.md) если что-то забудете.

---

> Хороший UI-компонент похож на качественный HTML-тег:  
> Он делает одну вещь, делает её хорошо, и не спрашивает лишних вопросов.