<?php

declare(strict_types=1);

namespace OlegV;

/**
 * Мутабельный аналог Brick (используйте с осторожностью)
 *
 * ⚠️ Предназначен ТОЛЬКО для особых случаев.
 *
 * Для HTML-рендеринга иммутабельность критически важна:
 * - Детерминированный вывод (одинаковые данные = одинаковый HTML)
 * - Безопасный параллельный рендеринг
 * - Эффективное кэширование
 * - Предсказуемое поведение компонентов
 *
 * Clay нарушает эти принципы. Используйте только когда:
 * 1. Требуется сложное внутреннее состояние
 * 2. Интеграция с legacy-кодом
 *
 * @see Brick Рекомендуемый иммутабельный вариант
 */
abstract class Clay
{
    use Mold;
}