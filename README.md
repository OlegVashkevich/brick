# 🧱 Brick - PHP UI Component Library

## 📋 Меню

- [Обзор](#обзор)
- [✨ Особенности](#-особенности)
- [📦 Установка](#-установка)
- [🚀 Быстрый старт](#-быстрый-старт)
    - [Создание компонента](#1-создание-компонента-button)
    - [Использование компонента](#2-использование-компонента)
- [🏗️ Архитектура компонента](#-архитектура-компонента)
    - [Структура файлов](#структура-файлов)
    - [Промоутед-свойства](#промоутед-свойства)
    - [Автоматическое обнаружение](#автоматическое-обнаружение-файлов)
- [🎨 Использование](#-использование)
    - [Рендеринг](#рендеринг-компонентов)
    - [Вложенность компонентов](#вложенность-компонентов)
    - [Хелперы в шаблонах](#хелперы-в-шаблонах)
    - [Наследование компонентов](#наследование-компонентов)
    - [Рендеринг компонентов с ассетами](#рендеринг-компонентов-с-ассетами)
- [⚡ Расширенные возможности](#-расширенные-возможности)
    - [Кэширование с PSR-16](#кэширование-с-psr-16)
    - [Расширенные хелперы](#расширенные-хелперы)
    - [Гибкое наследование](#гибкое-наследование)
    - [Оптимизация ассетов](#оптимизация-ассетов)
- [📊 Управление ассетами](#-управление-ассетами)
    - [InlineAssetRenderer](#inlineassetrenderer-по-умолчанию)
    - [FileAssetRenderer](#fileassetrenderer-для-продакшена)
    - [Режимы работы](#режимы-работы)
- [🔧 Интеграция](#-интеграция)
    - [С DI-контейнерами](#с-di-контейнерами)
    - [С PHP-шаблонизаторами](#с-php-шаблонизаторами)
- [🛡️ Надежность компонентов](#-надежность-компонентов)
    - [Тестирование](#тестирование)
    - [Статический анализ PHPStan](#статический-анализ-phpstan)
- [📚 API Справочник](#-api-справочник)
    - [Методы компонента](#методы-компонента)
    - [Статические методы](#статические-методы-brick)
    - [Трейты](#трейты)
    - [Интерфейсы](#интерфейсы)

---

## Обзор

**Brick** — это мощный инструмент для создания строго типизированных UI-компонентов на PHP. Он сочетает простоту использования для новичков с расширенными возможностями для опытных разработчиков.

**Философия Brick**: "UI как код" — компоненты становятся частью типизированной кодовой базы с четкими контрактами, а не шаблонным слоем вне статического анализа.

## ✨ Особенности

### 🎯 Для всех пользователей
- **Строгая типизация** — PHP 8.2+ с промоутед-свойствами, проверка на этапе компиляции
- **Автоматическое обнаружение** — класс, шаблон, стили и JS автоматически находятся вместе
- **Простое наследование** — расширяйте компоненты с кастомными шаблонами
- **Композиция** — типизированная вложенность компонентов
- **Авто-ассеты** — CSS/JS автоматически собираются и подключаются

### ⚡ Для продвинутых
- **PSR-16 кэширование** — встроенная поддержка кэширования для высокой производительности
- **Гибкие рендереры ассетов** — инлайн или файловый вывод с минификацией
- **Расширенные хелперы** — богатый набор методов для шаблонов
- **Глубокое наследование** — объединение стилей и скриптов в иерархии
- **PHPStan уровень max** — полная поддержка статического анализа

## 📦 Установка

```bash
composer require olegv/brick
```

Требования:
- PHP 8.2 или выше
- Composer

## 🚀 Быстрый старт

### 1. Создание компонента Button

**Структура файлов:**
```
src/Components/Button/
├── Button.php      # Класс компонента
├── template.php    # HTML шаблон
├── style.css       # Стили компонента
└── script.js       # JavaScript (опционально)
```

**Button.php:**
```php
<?php

namespace App\Components;

use OlegV\Brick;

class Button extends Brick
{
    public function __construct(
        public string $text,
        public string $variant = 'primary',
        public ?string $url = null,
        public bool $disabled = false,
    ) {
        parent::__construct(); // Автоматически находит все файлы
    }
}
```

**template.php:**
```php
<?php
/** @var \App\Components\Button $this */
$tag = $this->url && !$this->disabled ? 'a' : 'button';
?>

<<?= $tag ?>
    class="btn btn-<?= $this->e($this->variant) ?>"
    <?php if ($this->url && !$this->disabled): ?>
        href="<?= $this->e($this->url) ?>"
    <?php endif; ?>
    <?php if ($this->disabled && $tag === 'button'): ?>
        disabled
    <?php endif; ?>
>
    <?= $this->e($this->text) ?>
</<?= $tag ?>>
```

**style.css:**
```css
.btn {
    padding: 12px 24px;
    border-radius: 6px;
    font-weight: 500;
    cursor: pointer;
    border: none;
    transition: background-color 0.2s;
}

.btn-primary {
    background: #3b82f6;
    color: white;
}

.btn-primary:hover {
    background: #2563eb;
}

.btn:disabled {
    opacity: 0.6;
    cursor: not-allowed;
}
```

### 2. Использование компонента

```php
<?php

require 'vendor/autoload.php';

use App\Components\Button;

// Создание кнопки
$button = new Button(
    text: 'Нажми меня',
    variant: 'primary',
    url: '/products'
);

// Рендеринг компонента
echo $button; // Автоматически вызывает render()

// Вывод всех CSS стилей использованных компонентов
echo Brick::renderCss();
// <style>.btn { padding: 12px 24px; ... }</style>

// Вывод всех JavaScript скриптов
echo Brick::renderJs();
// <script>/* JS компонентов */</script>

// Или вывод всех ассетов сразу (CSS + JS)
echo Brick::renderAssets();
```

## 🏗️ Архитектура компонента

### Структура файлов
Каждый компонент состоит из 4 обязательных/опциональных файлов в одной папке:

1. **Класс компонента** (`ComponentName.php`) — PHP класс с логикой
2. **Шаблон** (`template.php`) — HTML с PHP вставками
3. **Стили** (`style.css`) — CSS только для этого компонента (опционально)
4. **Скрипты** (`script.js`) — JavaScript только для этого компонента (опционально)

### Промоутед-свойства
Используйте промоутед-свойства PHP 8.2+ для строгой типизации:

```php
class ProductCard extends Brick
{
    public function __construct(
        public int $id,              // Обязательное свойство
        public string $title,        // Обязательное свойство  
        public float $price,         // Обязательное свойство
        public ?string $description = null, // Опциональное
        public array $tags = [],     // Со значением по умолчанию
    ) {
        parent::__construct();
    }
}
```

### Автоматическое обнаружение файлов
Brick автоматически находит все файлы компонента в директории класса. Нет необходимости указывать пути вручную.

## 🎨 Использование

### Рендеринг компонентов

```php
// Способ 1: Приведение к строке (рекомендуется)
echo $button;

// Способ 2: Явный вызов render()
echo $button->render();

// Способ 3: В шаблоне другого компонента
<div class="container">
    <?= $button ?>
</div>
```

### Вложенность компонентов

```php
class ProductCard extends Brick
{
    public function __construct(
        public int $id,
        public string $title,
        public float $price,
        public Button $button, // ← Типизированная композиция!
    ) {
        parent::__construct();
    }
}

// Использование
$buyButton = new Button('Купить', 'primary');
$product = new ProductCard(1, 'Наушники', 99.99, $buyButton);

echo $product;
```

### Хелперы в шаблонах

```php
<!-- template.php -->
<div class="<?= $this->classList([
    'card',
    'card--featured' => $this->isFeatured,
    'card--sale' => $this->onSale
]) ?>">
    
    <h2><?= $this->e($this->title) ?></h2>
    
    <div class="price">
        <?= $this->number($this->price, 2) ?> ₽
    </div>
    
    <?php if ($this->description): ?>
        <p><?= $this->e($this->description) ?></p>
    <?php endif; ?>
    
</div>
```

### Наследование компонентов

```php
// Базовый компонент
abstract class BaseCard extends Brick
{
    public function __construct(
        public string $title,
        public array $classes = []
    ) {
        parent::__construct();
    }
}

// Расширенный компонент
class ProductCard extends BaseCard
{
    public function __construct(
        string $title,
        public float $price,
        public ?string $image = null
    ) {
        parent::__construct($title);
    }
}

// ProductCard автоматически найдет template.php в своей папке
```

### Рендеринг компонентов с ассетами

```php
// В контроллере или шаблоне
<?php

use App\Components\Button;
use App\Components\ProductCard;

// Создаем компоненты
$buyButton = new Button('Купить', 'primary');
$product = new ProductCard(
    id: 1,
    title: 'Наушники',
    price: 99.99,
    button: $buyButton
);

?>

<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Магазин</title>
    
    <!-- Выводим все CSS стили использованных компонентов -->
    <?= Brick::renderCss() ?>
</head>
<body>
    
    <header>
        <!-- Рендерим компоненты -->
        <?= $product ?>
    </header>
    
    <!-- Выводим все JavaScript скрипты в конце страницы -->
    <?= Brick::renderJs() ?>
    
</body>
</html>
```

### Важные замечания:

1. **Ассеты собираются автоматически** — когда вы создаете компонент, его CSS/JS автоматически добавляются в реестр
2. **Дедупликация** — если один компонент используется несколько раз, его ассеты подключатся только один раз
3. **Порядок подключения** — CSS выводится в `<head>`, JS рекомендуется выводить перед закрывающим `</body>`
4. **Очистка для тестов** — используйте `Brick::clear()` в тестах для очистки реестра ассетов
5. **Наследование ассетов** — при использовании трейта `WithInheritance` ассеты объединяются у всех компонентов в цепочке наследования

Это делает работу с ассетами максимально простой — Brick автоматически заботится о сборе, дедупликации и выводе всех необходимых стилей и скриптов.

## ⚡ Расширенные возможности

### Кэширование с PSR-16

```php
use OlegV\Traits\WithCache;
use Symfony\Component\Cache\Psr16Cache;

class ProductCard extends Brick
{
    use WithCache; // ← Подключаем трейт кэширования
}

// Настройка кэша
$cache = new Psr16Cache(
    new FilesystemAdapter('brick_cache', 3600)
);

ProductCard::setCache($cache);

// Теперь рендеринг автоматически кэшируется
$card = new ProductCard(...);
echo $card; // Первый раз - рендеринг, второй - из кэша
```

### Расширенные хелперы

```php
use OlegV\Traits\WithHelpers;

class AdvancedComponent extends Brick
{
    use WithHelpers; // ← Подключаем расширенные хелперы
    
    public function formatDate(): string
    {
        return $this->date($this->createdAt, 'd.m.Y H:i');
    }
}

// В шаблоне доступны:
$this->e($value);           // Экранирование HTML
$this->attr($attributes);   // Генерация атрибутов
$this->date($timestamp);    // Форматирование даты
$this->number($price, 2);   // Форматирование чисел
$this->json($data);         // Безопасный JSON
$this->truncate($text, 50); // Обрезка текста
$this->plural(5, $forms);   // Множественное число
$this->url($path, $params); // Генерация URL
```

### Гибкое наследование

```php
use OlegV\Traits\WithInheritance;

class IconButton extends PrimaryButton
{
    use WithInheritance; // ← Мощное наследование
    
    public function __construct(
        public string $text,
        public string $icon
    ) {
        parent::__construct($text);
    }
}

// WithInheritance автоматически:
// 1. Найдет template.php в иерархии наследования
// 2. Объединит все style.css от родителей
// 3. Объединит все script.js от родителей
```

### Оптимизация ассетов

```php
use OlegV\Assets\FileAssetRenderer;

$renderer = new FileAssetRenderer(
    outputDir: __DIR__ . '/public/assets',
    publicUrl: '/assets/',
    minify: true,          // Включить минификацию
    mode: FileAssetRenderer::MODE_SINGLE, // Один файл для всех
    filePrefix: 'app'
);

Brick::setAssetRenderer($renderer);

// Теперь ассеты выводятся как:
// <link rel="stylesheet" href="/assets/app.all.abc123.css">
// <script src="/assets/app.all.def456.js"></script>
```

## 📊 Управление ассетами

### InlineAssetRenderer (по умолчанию)
Встраивает CSS и JS напрямую в HTML:

```html
<style>
/* Все CSS компонентов */
</style>

<script>
/* Все JS компонентов */
</script>
```

### FileAssetRenderer (для продакшена)
Выгружает ассеты в отдельные файлы:

```php
$renderer = new FileAssetRenderer(
    __DIR__ . '/public/assets', // Куда сохранять
    '/assets/',                 // Публичный URL
    true,                       // Минифицировать
    FileAssetRenderer::MODE_MULTIPLE // Режим
);
```

**Преимущества:**
- Кэширование браузером
- Минификация для уменьшения размера
- Возможность CDN
- Разделение на несколько файлов

### Режимы работы

```php
// MODE_SINGLE - один файл для всех компонентов
// /assets/brick.all.abc123.css

// MODE_MULTIPLE - отдельный файл для каждого компонента  
// /assets/brick.button.def456.css
// /assets/brick.card.ghi789.css
```

## 🔧 Интеграция

### С DI-контейнерами

Brick компоненты легко работают с любым DI-контейнером благодаря конструкторному инжекту зависимостей. Каждый компонент принимает все зависимости через конструктор, что делает его идеальным для PSR-11 совместимых контейнеров.

**Простой пример с DI:**
```php
$container->set(Button::class, fn() => new Button('Купить', 'primary'));
$button = $container->get(Button::class);
```

**💡 Для минималистичных проектов** рассмотрите [Cement](https://github.com/OlegVashkevich/cement) — DI-контейнер, созданный специально для работы с Brick.

### С PHP-шаблонизаторами

Компоненты Brick легко интегрируются с любыми PHP шаблонизаторами (Blade, Twig, Smarty и др.). Просто используйте метод `render()` или приведение к строке:

```php
// В любом шаблоне
echo $component; // или $component->render()
```

**Быстрая интеграция:**
- **Blade**: добавьте директиву `@brick($component)`
- **Twig**: добавьте функцию `{{ brick(component)|raw }}`
- **Любой другой**: используйте `$component->render()` напрямую

Компоненты Brick дополняют существующие шаблонизаторы, добавляя строгую типизацию и переиспользуемость.

## 🛡️ Надежность компонентов

### Тестирование

Компоненты Brick — это обычные PHP классы, поэтому их можно тестировать стандартными инструментами:

```php
use PHPUnit\Framework\TestCase;

class ButtonTest extends TestCase
{
    public function testButtonRendersCorrectly(): void
    {
        $button = new Button('Test', 'primary');
        $html = $button->render();
        
        $this->assertStringContainsString('Test', $html);
        $this->assertStringContainsString('btn-primary', $html);
        $this->assertStringContainsString('<button', $html);
    }
    
    public function testButtonWithUrlRendersAsLink(): void
    {
        $button = new Button('Link', 'primary', '/test');
        $html = $button->render();
        
        $this->assertStringContainsString('<a', $html);
        $this->assertStringContainsString('href="/test"', $html);
    }
}
```

**Особенности тестирования:**
- Используйте `Brick::clear()` для очистки ассетов между тестами
- Тестируйте как интеграцию (рендеринг HTML), так и юниты (логику компонента)
- Проверяйте граничные случаи: пустые значения, особые состояния

Компоненты Brick легко интегрируются с PHPUnit и другими фреймворками для тестирования.

### Статический анализ PHPStan

Brick — это не просто шаблонизатор, а инструмент для создания **полностью анализируемых UI-компонентов**.

#### Что анализируется?

**1. Класс компонента (100% покрытие PHPStan):**
```php
// ✅ Все типы проверяются на этапе компиляции
class ProductCard extends Brick
{
    public function __construct(
        public int $id,           // PHPStan знает тип
        public string $title,     // PHPStan знает тип
        public float $price,      // PHPStan знает тип
        public ?string $image = null // PHPStan знает тип
    ) {
        parent::__construct();
    }
}

// ❌ Ошибка на этапе анализа:
// $card = new ProductCard('не число', 123); // Type mismatch
```

**2. Шаблон template.php:**
```php
<!-- PHPStan может анализировать PHP-код в шаблоне -->
<?php
/** @var \App\Components\ProductCard $this */
// Аннотация помогает PHPStan понимать тип $this
?>

<div class="card">
    <h2><?= $this->e($this->title) ?></h2>
    
    <!-- PHPStan проверяет существование свойства -->
    <?php if ($this->image): ?> 
    <!-- ✅ Свойство image существует и имеет тип ?string -->
        <img src="<?= $this->e($this->image) ?>" alt="">
    <?php endif; ?>
    
    <!-- ❌ PHPStan поймает ошибку, если обратиться к несуществующему свойству -->
    <!-- <?= $this->nonExistentProperty ?> -->
</div>
```

#### Преимущества статического анализа с Brick:

✅ **Раннее обнаружение ошибок** — опечатки в именах свойств обнаруживаются до запуска  
✅ **Проверка типов** — гарантия, что в компонент переданы правильные данные  
✅ **Рефакторинг** — безопасное переименование свойств с поддержкой IDE  
✅ **Автодополнение** — IDE понимает структуру компонента и предлагает свойства  
✅ **Документация** — типы сами документируют API компонента

#### Рекомендации:

1. **Всегда указывайте типы** — и в классе, и в phpdoc шаблона
2. **Используйте промоутед-свойства** — для явного объявления API компонента
3. **Тестируйте рендеринг** — дополняйте статический анализ юнит-тестами
4. **Проверяйте регулярно** — добавляйте PHPStan в CI/CD пайплайн

Brick превращает UI-компоненты из "чёрного ящика" в **полноценные, анализируемые единицы кода**, что значительно повышает надёжность и поддерживаемость интерфейсов.

## 📚 API Справочник

### Методы компонента

| Метод                                  | Описание | Пример |
|----------------------------------------|----------|---------|
| `render(): string`                     | Рендерит компонент в HTML | `$html = $button->render()` |
| `__toString(): string`                 | Автоматически вызывает render() | `echo $button` |

### Статические методы Brick

| Метод                                  | Описание | Пример |
|----------------------------------------|----------|---------|
| `renderCss(): string`                  | Рендерит все CSS компонентов | `echo Brick::renderCss()` |
| `renderJs(): string`                   | Рендерит весь JS компонентов | `echo Brick::renderJs()` |
| `renderAssets(): string`               | Рендерит CSS + JS | `echo Brick::renderAssets()` |
| `setAssetRenderer(AssetRenderer): void` | Устанавливает рендерер ассетов | `Brick::setAssetRenderer($renderer)` |
| `clear(): void`                        | Очищает ассеты (для тестов) | `Brick::clear()` |

### Трейты

| Трейт | Назначение | Когда использовать |
|-------|------------|-------------------|
| `WithCache` | PSR-16 кэширование рендеринга | Для высоконагруженных приложений |
| `WithHelpers` | Расширенные хелперы для шаблонов | Для сложных шаблонов с форматированием |
| `WithInheritance` | Мощное наследование компонентов | Для глубоких иерархий компонентов |

### Интерфейсы

| Интерфейс | Назначение | Реализации |
|-----------|------------|------------|
| `AssetRenderer` | Рендеринг ассетов | `InlineAssetRenderer`, `FileAssetRenderer` |
| `CacheInterface` | PSR-16 кэширование | Любая PSR-16 совместимая реализация |

---

**Brick** — минимальная абстракция, максимальная предсказуемость. Создавайте UI с четкими контрактами и полной поддержкой статического анализа.