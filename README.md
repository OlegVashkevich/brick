# 🧱 Brick - PHP UI Component Library Tool v2.0

## 📋 Меню

- [Обзор](#обзор)
- [✨ Что нового в v2.0?](#-что-нового-в-v20)
- [📦 Установка](#-установка)
- [🚀 Быстрый старт](#-быстрый-старт)
    - [Создание компонента](#1-создание-компонента-button)
    - [Использование компонента](#2-использование-компонента)
- [🏗️ Архитектура компонента](#-архитектура-компонента)
    - [Структура файлов](#структура-файлов)
    - [Промоутед-свойства и readonly](#промоутед-свойства-и-readonly)
    - [Автоматическое обнаружение](#автоматическое-обнаружение-файлов)
- [🎨 Использование](#-использование)
    - [Рендеринг](#рендеринг-компонентов)
    - [Вложенность компонентов](#вложенность-компонентов)
    - [Хелперы в шаблонах](#хелперы-в-шаблонах)
    - [Наследование компонентов](#наследование-компонентов)
- [📊 Управление ассетами](#-управление-ассетами)
    - [BrickManager - центральный менеджер](#brickmanager---центральный-менеджер)
    - [InlineAssetRenderer](#inlineassetrenderer-по-умолчанию)
    - [FileAssetRenderer](#fileassetrenderer-для-продакшена)
    - [Режимы работы](#режимы-работы)
- [⚡ Расширенные возможности](#-расширенные-возможности)
    - [Кэширование с PSR-16](#кэширование-с-psr-16)
    - [Расширенные хелперы](#расширенные-хелперы)
    - [Гибкое наследование](#гибкое-наследование)
    - [Оптимизация ассетов](#оптимизация-ассетов)
- [🔧 Интеграция](#-интеграция)
    - [С DI-контейнерами](#с-di-контейнерами)
    - [С PHP-шаблонизаторами](#с-php-шаблонизаторами)
- [🛡️ Надежность компонентов](#-надежность-компонентов)
    - [Тестирование](#тестирование)
    - [Статический анализ PHPStan](#статический-анализ-phpstan)
- [📚 API Справочник](#-api-справочник)
    - [Методы компонента](#методы-компонента)
    - [BrickManager](#brickmanager)
    - [Трейты](#трейты)
    - [Интерфейсы и классы ассетов](#интерфейсы-и-классы-ассетов)

---

## Обзор

**Brick v2.0** — это мощный инструмент для создания строго типизированных, иммутабельных UI-компонентов на PHP. Он сочетает простоту использования для новичков с расширенными возможностями для опытных разработчиков.

**Философия Brick**: "UI как код" — компоненты становятся частью типизированной кодовой базы с четкими контрактами, а не шаблонным слоем вне статического анализа.

## ✨ Что нового в v2.0?

### 🎯 Основные изменения
- **BrickManager** — централизованное управление компонентами и ассетами
- **readonly компоненты** — все компоненты теперь иммутабельные (`readonly class`)
- **Улучшенная архитектура** — разделение ответственности между классами
- **Трейты вместо наследования** — гибкое подключение возможностей

### ⚡ Новые возможности
- **Гибкое управление ассетами** — через `BrickManager` с поддержкой разных рендереров
- **Продвинутое кэширование** — трейт `WithCache` с PSR-16 поддержкой
- **Расширенные хелперы** — трейт `WithHelpers` для сложных шаблонов
- **Глубокое наследование** — трейт `WithInheritance` для иерархий компонентов

## 📦 Установка

```bash
composer require olegv/brick
```

Требования:
- PHP 8.2 или выше (требуется для `readonly class`)
- Composer

## 🚀 Быстрый старт

### 1. Создание компонента Button

**Структура файлов:**
```
src/Components/Button/
├── Button.php      # Класс компонента
├── template.php    # HTML шаблон
├── style.css       # Стили компонента
└── script.js       # JavaScript (опционально)
```

**Button.php:**
```php
<?php

declare(strict_types=1);

namespace App\Components;

use OlegV\Brick;

readonly class Button extends Brick
{
    public function __construct(
        public string $text,
        public string $variant = 'primary',
        public ?string $url = null,
        public bool $disabled = false,
    ) {
        parent::__construct(); // Автоматически находит все файлы
    }
}
```

**template.php:**
```php
<?php
/** @var \App\Components\Button $this */
$tag = $this->url && !$this->disabled ? 'a' : 'button';
?>

<<?= $tag ?>
    class="btn btn-<?= $this->e($this->variant) ?>"
    <?php if ($this->url && !$this->disabled): ?>
        href="<?= $this->e($this->url) ?>"
    <?php endif; ?>
    <?php if ($this->disabled && $tag === 'button'): ?>
        disabled
    <?php endif; ?>
>
    <?= $this->e($this->text) ?>
</<?= $tag ?>>
```

**style.css:**
```css
.btn {
    padding: 12px 24px;
    border-radius: 6px;
    font-weight: 500;
    cursor: pointer;
    border: none;
    transition: background-color 0.2s;
}

.btn-primary {
    background: #3b82f6;
    color: white;
}

.btn-primary:hover {
    background: #2563eb;
}

.btn:disabled {
    opacity: 0.6;
    cursor: not-allowed;
}
```

### 2. Использование компонента

```php
<?php

require 'vendor/autoload.php';

use App\Components\Button;
use OlegV\BrickManager;

// Создание кнопки
$button = new Button(
    text: 'Нажми меня',
    variant: 'primary',
    url: '/products'
);

// Рендеринг компонента
echo $button; // Автоматически вызывает render()

// Получаем менеджер
$manager = BrickManager::getInstance();

// Вывод всех CSS стилей использованных компонентов
echo $manager->renderCss();
// <style>.btn { padding: 12px 24px; ... }</style>

// Вывод всех JavaScript скриптов
echo $manager->renderJs();
// <script>/* JS компонентов */</script>

// Или вывод всех ассетов сразу (CSS + JS)
echo $manager->renderAssets();
```

## 🏗️ Архитектура компонента

### Структура файлов
Каждый компонент состоит из 4 обязательных/опциональных файлов в одной папке:

1. **Класс компонента** (`ComponentName.php`) — PHP класс с логикой
2. **Шаблон** (`template.php`) — HTML с PHP вставками
3. **Стили** (`style.css`) — CSS только для этого компонента (опционально)
4. **Скрипты** (`script.js`) — JavaScript только для этого компонента (опционально)

### Промоутед-свойства и readonly
Используйте промоутед-свойства PHP 8.2+ для строгой типизации и `readonly` для иммутабельности:

```php
readonly class ProductCard extends Brick
{
    public function __construct(
        public int $id,              // Обязательное свойство
        public string $title,        // Обязательное свойство  
        public float $price,         // Обязательное свойство
        public ?string $description = null, // Опциональное
        public array $tags = [],     // Со значением по умолчанию
    ) {
        parent::__construct();
    }
}
```

**Преимущества readonly:**
- ✅ Компоненты иммутабельны после создания
- ✅ Предсказуемое поведение
- ✅ Безопасность в многопоточных средах
- ✅ Лучшая производительность

### Автоматическое обнаружение файлов
Brick автоматически находит все файлы компонента в директории класса. Нет необходимости указывать пути вручную.

## 🎨 Использование

### Рендеринг компонентов

```php
// Способ 1: Приведение к строке (рекомендуется)
echo $button;

// Способ 2: Явный вызов render()
echo $button->render();

// Способ 3: В шаблоне другого компонента
<div class="container">
    <?= $button ?>
</div>
```

### Вложенность компонентов

```php
readonly class ProductCard extends Brick
{
    public function __construct(
        public int $id,
        public string $title,
        public float $price,
        public Button $button, // ← Типизированная композиция!
    ) {
        parent::__construct();
    }
}

// Использование
$buyButton = new Button('Купить', 'primary');
$product = new ProductCard(1, 'Наушники', 99.99, $buyButton);

echo $product;
```

### Хелперы в шаблонах

```php
<!-- template.php -->
<div class="<?= $this->classList([
    'card',
    'card--featured' => $this->isFeatured,
    'card--sale' => $this->onSale
]) ?>">
    
    <h2><?= $this->e($this->title) ?></h2>
    
    <div class="price">
        <?= $this->number($this->price, 2) ?> ₽
    </div>
    
    <?php if ($this->description): ?>
        <p><?= $this->e($this->description) ?></p>
    <?php endif; ?>
    
</div>
```

### Наследование компонентов

```php
// Базовый компонент
abstract readonly class BaseCard extends Brick
{
    public function __construct(
        public string $title,
        public array $classes = []
    ) {
        parent::__construct();
    }
}

// Расширенный компонент
readonly class ProductCard extends BaseCard
{
    public function __construct(
        string $title,
        public float $price,
        public ?string $image = null
    ) {
        parent::__construct($title);
    }
}

// ProductCard автоматически найдет template.php в своей папке
```

## 📊 Управление ассетами

### BrickManager - центральный менеджер

`BrickManager` — это сердце Brick v2.0. Он управляет всеми компонентами и их ассетами:

```php
use OlegV\BrickManager;

// Получение экземпляра менеджера
$manager = BrickManager::getInstance();

// Настройка рендерера ассетов
$renderer = new FileAssetRenderer(__DIR__ . '/public/assets');
$manager = new BrickManager($renderer);

// Управление ассетами
echo $manager->renderCss();    // Все CSS компонентов
echo $manager->renderJs();     // Все JS компонентов
echo $manager->renderAssets(); // CSS + JS вместе

// Очистка (полезно для тестов)
$manager->clear();

// Статистика
$stats = $manager->getStats();
// ['cached_classes' => 5, 'css_assets' => 3, 'js_assets' => 2]
```

### InlineAssetRenderer (по умолчанию)
Встраивает CSS и JS напрямую в HTML:

```html
<style>
/* Все CSS компонентов */
</style>

<script>
/* Все JS компонентов */
</script>
```

### FileAssetRenderer (для продакшена)
Выгружает ассеты в отдельные файлы:

```php
use OlegV\Assets\FileAssetRenderer;

$renderer = new FileAssetRenderer(
    outputDir: __DIR__ . '/public/assets', // Куда сохранять
    publicUrl: '/assets/',                 // Публичный URL
    minify: true,                          // Минифицировать
    mode: FileAssetRenderer::MODE_MULTIPLE // Режим
    filePrefix: 'app'                      // Префикс файлов
);
```

**Преимущества:**
- Кэширование браузером
- Минификация для уменьшения размера
- Возможность CDN
- Разделение на несколько файлов

### Режимы работы

```php
// MODE_SINGLE - один файл для всех компонентов
// /assets/brick.all.abc123.css

// MODE_MULTIPLE - отдельный файл для каждого компонента  
// /assets/brick.button.def456.css
// /assets/brick.card.ghi789.css
```

## ⚡ Расширенные возможности

### Кэширование с PSR-16

```php
use OlegV\Traits\WithCache;
use Symfony\Component\Cache\Psr16Cache;
use OlegV\BrickManager;

readonly class ProductCard extends Brick
{
    use WithCache; // ← Подключаем трейт кэширования
    
    //если нужно указать особое время кэша
    protected function getTtl(): int
    {
        return 600; //время кэша изменено
    }
}

// Настройка кэша через BrickManager
$cache = new Psr16Cache(
    new FilesystemAdapter('brick_cache', 3600)
);

BrickManager::setCache($cache);

// Настройка параметров кэширования
BrickManager::$cachePrefix = 'myapp_';
BrickManager::$cacheTtl = 7200; // 2 часа

// Теперь рендеринг автоматически кэшируется
$card = new ProductCard(...);
echo $card; // Первый раз - рендеринг, второй - из кэша
```

### Расширенные хелперы

```php
use OlegV\Traits\WithHelpers;

readonly class AdvancedComponent extends Brick
{
    use WithHelpers; // ← Подключаем расширенные хелперы
    
    public function formatDate(): string
    {
        return $this->date($this->createdAt, 'd.m.Y H:i');
    }
}

// В шаблоне доступны все методы из WithHelpers:
$this->e($value);           // Экранирование HTML
$this->attr($attributes);   // Генерация атрибутов
$this->classList($classes); // Генерация классов
$this->date($timestamp);    // Форматирование даты
$this->number($price, 2);   // Форматирование чисел
$this->json($data);         // Безопасный JSON
$this->truncate($text, 50); // Обрезка текста
$this->plural(5, $forms);   // Множественное число
$this->url($path, $params); // Генерация URL
$this->uniqueId($prefix);   // Уникальный ID
$this->wordCount($text);    // Подсчет слов
```

### Гибкое наследование

```php
use OlegV\Traits\WithInheritance;

readonly class IconButton extends PrimaryButton
{
    use WithInheritance; // ← Мощное наследование
    
    public function __construct(
        public string $text,
        public string $icon
    ) {
        parent::__construct($text);
    }
}

// WithInheritance автоматически:
// 1. Найдет template.php в иерархии наследования
// 2. Объединит все style.css от родителей
// 3. Объединит все script.js от родителей
```

### Оптимизация ассетов

```php
use OlegV\Assets\FileAssetRenderer;
use OlegV\BrickManager;

$renderer = new FileAssetRenderer(
    outputDir: __DIR__ . '/public/assets',
    publicUrl: '/assets/',
    minify: true,          // Включить минификацию
    mode: FileAssetRenderer::MODE_SINGLE, // Один файл для всех
    filePrefix: 'app'
);

// Создаем менеджер с кастомным рендерером
$manager = new BrickManager($renderer);

// Или устанавливаем глобально (если используете singleton)
BrickManager::$instance = new BrickManager($renderer);

// Теперь ассеты выводятся как:
// <link rel="stylesheet" href="/assets/app.all.abc123.css">
// <script src="/assets/app.all.def456.js"></script>
```

## 🔧 Интеграция

### С DI-контейнерами

Brick компоненты легко работают с любым DI-контейнером благодаря конструкторному инжекту зависимостей и иммутабельности.

**Пример с PHP-DI:**
```php
use DI\ContainerBuilder;

$builder = new ContainerBuilder();
$builder->addDefinitions([
    Button::class => fn() => new Button('Купить', 'primary'),
    ProductCard::class => fn(ContainerInterface $c) => new ProductCard(
        id: 1,
        title: 'Наушники',
        price: 99.99,
        button: $c->get(Button::class)
    ),
]);

$container = $builder->build();
$card = $container->get(ProductCard::class);
```

**💡 Для минималистичных проектов** рассмотрите [Cement](https://github.com/OlegVashkevich/cement) — DI-контейнер, созданный специально для работы с Brick.

### С PHP-шаблонизаторами

Компоненты Brick легко интегрируются с любыми PHP шаблонизаторами (Blade, Twig, Smarty и др.):

```php
// Blade шаблон
@php
    $button = new Button('Купить', 'primary');
@endphp

<div class="container">
    {!! $button !!}
</div>
```

**Быстрая интеграция:**
- **Blade**: `{!! $component !!}` или создайте директиву `@brick()`
- **Twig**: `{{ component|raw }}` или добавьте функцию
- **Любой другой**: используйте `$component->render()` напрямую

## 🛡️ Надежность компонентов

### Тестирование

Компоненты Brick — это обычные PHP классы, поэтому их можно тестировать стандартными инструментами:

```php
use PHPUnit\Framework\TestCase;
use OlegV\BrickManager;

class ButtonTest extends TestCase
{
    protected function setUp(): void
    {
        // Очищаем менеджер перед каждым тестом
        BrickManager::getInstance()->clear();
    }
    
    public function testButtonRendersCorrectly(): void
    {
        $button = new Button('Test', 'primary');
        $html = $button->render();
        
        $this->assertStringContainsString('Test', $html);
        $this->assertStringContainsString('btn-primary', $html);
        $this->assertStringContainsString('<button', $html);
    }
    
    public function testButtonWithUrlRendersAsLink(): void
    {
        $button = new Button('Link', 'primary', '/test');
        $html = $button->render();
        
        $this->assertStringContainsString('<a', $html);
        $this->assertStringContainsString('href="/test"', $html);
    }
}
```

### Статический анализ PHPStan

Brick v2.0 поддерживает PHPStan на максимальном уровне:

```php
// ✅ Полная проверка типов
readonly class ProductCard extends Brick
{
    public function __construct(
        public int $id,           // PHPStan проверяет тип
        public string $title,     // PHPStan проверяет тип
        public float $price,      // PHPStan проверяет тип
    ) {
        parent::__construct();
    }
}

// ❌ Ошибка на этапе анализа:
// $card = new ProductCard('не число', 123, 'строка вместо числа');
```

**Преимущества:**
- Раннее обнаружение ошибок
- Проверка типов при компиляции
- Безопасный рефакторинг
- Автодополнение в IDE
- Самодокументирующийся код

## 📚 API Справочник

### Методы компонента

| Метод | Описание | Пример |
|-------|----------|---------|
| `render(): string` | Рендерит компонент в HTML | `$html = $button->render()` |
| `__toString(): string` | Автоматически вызывает render() | `echo $button` |
| `initializeComponent(BrickManager): void` | Переопределяется для кастомной инициализации | См. WithInheritance |

### BrickManager

| Метод | Описание | Пример |
|-------|----------|---------|
| `getInstance(): self` | Получить singleton инстанс | `$manager = BrickManager::getInstance()` |
| `setCache(CacheInterface): void` | Установить PSR-16 кэш | `BrickManager::setCache($cache)` |
| `getCache(): ?CacheInterface` | Получить текущий кэш | `$cache = BrickManager::getCache()` |
| `renderCss(): string` | Рендерить все CSS | `echo $manager->renderCss()` |
| `renderJs(): string` | Рендерить все JS | `echo $manager->renderJs()` |
| `renderAssets(): string` | Рендерить CSS + JS | `echo $manager->renderAssets()` |
| `clear(): void` | Очистить все данные | `$manager->clear()` |
| `getStats(): array` | Получить статистику | `$stats = $manager->getStats()` |

**Статические свойства BrickManager:**
- `$cachePrefix` - префикс для ключей кэша (по умолчанию: 'brick_')
- `$cacheTtl` - время жизни кэша в секундах (по умолчанию: 3600)

### Трейты

| Трейт | Назначение | Когда использовать |
|-------|------------|-------------------|
| `WithCache` | PSR-16 кэширование рендеринга | Для высоконагруженных приложений |
| `WithHelpers` | Расширенные хелперы для шаблонов | Для сложных шаблонов с форматированием |
| `WithInheritance` | Мощное наследование компонентов | Для глубоких иерархий компонентов |

### Интерфейсы и классы ассетов

| Класс/Интерфейс | Назначение | Реализации |
|-----------------|------------|------------|
| `AssetRenderer` | Интерфейс для рендереров ассетов | - |
| `AbstractAssetRenderer` | Базовый класс для рендереров | - |
| `InlineAssetRenderer` | Встраивает ассеты в HTML | По умолчанию |
| `FileAssetRenderer` | Сохраняет ассеты в файлы | Для продакшена |

**Методы AbstractAssetRenderer:**
- `setMode(string): void` - установить режим (MODE_SINGLE/MODE_MULTIPLE)
- `setMinify(bool): void` - включить/выключить минификацию
- `getMode(): string` - получить текущий режим
- `isMinify(): bool` - проверка минификации
- `getAvailableModes(): array` - список доступных режимов

---

**Brick v2.0** — современный инструмент для создания предсказуемых, типизированных UI-компонентов с полной поддержкой статического анализа и иммутабельностью.